name: Template Setup

permissions:
  contents: write

on:
  push:
    branches: [ prod ]
  workflow_dispatch:
    inputs:
      project_name:
        description: '새 프로젝트 이름'
        required: true
        type: string

jobs:
  setup:
    runs-on: ubuntu-latest
    if: github.event.repository.name != 'actions-test'
    steps:
      - uses: actions/checkout@v4
      
      - name: Get project name
        id: project
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "name=${{ github.event.inputs.project_name }}" >> $GITHUB_OUTPUT
          else
            echo "name=${{ github.event.repository.name }}" >> $GITHUB_OUTPUT
          fi

      - name: Setup project
        run: |
          chmod +x ./setup.sh
          ./setup.sh "${{ steps.project.outputs.name }}" "${{ steps.project.outputs.name }}"
        
      - name: Commit changes
        run: |
          git push -f origin prod
